1. Number Sequence - id: "cms_count_number_sequence_v1"
{"regions": [{"id": "header", "components": [{"id": "skillLabel", "type": "text", "editable": true, "description": "Label naming the skill/activity being taught on this flashcard (e.g. 'Number Sequence 1-10'). Must match the actual content generated below.", "semanticRole": "phonics.skill.label"}, {"id": "brandingLogo", "type": "image", "content": "ab_logo.png", "editable": false, "description": "Static brand logo; not generated by the content model.", "semanticRole": "branding.logo"}]}, {"id": "numbers", "range": {"max": 100, "min": 10, "note": "count = end - start + 1. If the user gives no range, default to a count of 10 (e.g. 1-10). Clamp any user-given count to [10, 100].", "default": {"end": 10, "start": 1}}, "layout": {"type": "grid", "cellBounds": {"maxCells": 50, "minCells": 10}, "pagination": "Allowed when count > 50. Each page is an independent region using this same layout logic.", "selectionRule": "Use cols = 5 whenever the page can be represented with 5 columns and between 5 and 10 rows. Otherwise use the smallest column count in [2, 5] that produces a rectangular grid with rows in [5, 10]. rows = count / cols. Every page must have exactly rows * cols cells with no empty trailing cells. For counts greater than 50, split the sequence into multiple sequential pages, each containing at most 50 cells and following the same layout rules. Each page must be a complete rectangular grid.", "dimensionBounds": {"max": {"cols": 5, "rows": 10}, "min": {"cols": 2, "rows": 5}}}, "components": [{"id": "num-{x}", "type": "text", "editable": true, "constraints": {"maxCharacters": 3}, "description": "One number in the requested range, generated in order. Omit exactly one number at random within each page's range and render it as an underscore/blank so the learner can fill it in.", "semanticRole": "counting.sequence.item"}], "constraints": {"note": "Item count per page must exactly equal that page's rows*cols (layout.cells). No partial rows.", "itemCountMustEqual": "layout.cells"}}]}

2. Read the numbers - id: "cms_read_numbers_v1"
{"regions": [{"id": "header", "components": [{"id": "skillLabel", "type": "text", "editable": true, "description": "Label naming the skill/activity being taught on this flashcard (e.g. 'Read the Numbers'). Must match the actual content generated below.", "semanticRole": "phonics.skill.label"}, {"id": "brandingLogo", "type": "image", "content": "ab_logo.png", "editable": false, "description": "Static brand logo; not generated by the content model.", "semanticRole": "branding.logo"}]}, {"id": "numbers", "range": {"end": {"max": 999, "min": 0, "default": 10}, "note": "The user may request any consecutive numeric range where start <= end and count = end - start + 1. The count must be between 1 and 10. If no range is provided, use the default range 1-10. If the requested count exceeds 10, clamp or reduce the range to a maximum of 10 numbers.", "start": {"max": 999, "min": 0, "default": 1}, "default": {"end": 10, "start": 1}, "maxCount": 10}, "layout": {"cols": 2, "note": "Each grid cell contains one numeral with its spelled-out reading directly below it. Populate cells left-to-right, top-to-bottom. The grid always remains 5 rows by 2 columns. If fewer than 10 numbers are requested, unused cells remain empty or hidden.", "rows": 5, "type": "grid", "cells": 10, "responsive": true, "cellContentLayout": "vertical"}, "components": [{"id": "number-{x}", "type": "text", "editable": true, "constraints": {"maxCharacters": 3}, "description": "The numeral for the x-th item in the requested range, generated sequentially from start through end.", "semanticRole": "numbers.digit.item"}, {"id": "reading-{x}", "type": "text", "editable": true, "constraints": {"maxCharacters": 15}, "description": "The English word form of the paired number-{x} component, displayed directly below the numeral. For example, number '7' must have reading 'seven'.", "semanticRole": "numbers.reading.item"}], "constraints": {"maxItems": 10, "minItems": 1, "rangeRule": "start >= 0, end <= 999, start <= end, and count <= 10", "layoutRule": "Maximum 2 columns and maximum 5 rows. The fixed grid capacity is exactly 10 number/reading pairs.", "pairingRule": "Each number-{x} must have exactly one corresponding reading-{x}. The reading must be the correct spelled-out form of the paired numeral.", "orderingRule": "Generate numbers sequentially from start to end and place them left-to-right, then top-to-bottom.", "itemCountRule": "count = end - start + 1"}}]}

3. Sight Words - id: "cms_sight_words_grid_v1"
{"regions": [{"id": "header", "components": [{"id": "skillLabel", "type": "text", "editable": true, "description": "Label naming the skill/activity being taught on this flashcard (e.g. 'Sight Words'). Must match the actual content generated below.", "semanticRole": "phonics.skill.label"}, {"id": "brandingLogo", "type": "image", "content": "ab_logo.png", "editable": false, "description": "Static brand logo; not generated by the content model.", "semanticRole": "branding.logo"}]}, {"id": "grid", "layout": {"type": "grid", "default": "GRID_2X3", "options": [{"cols": 4, "name": "GRID_1X4", "rows": 1, "cells": 4}, {"cols": 3, "name": "GRID_2X3", "rows": 2, "cells": 6}], "selectionRule": "Use GRID_1X4 (4 words) for younger/beginner age groups (e.g. LKG-UKG / ~2-4). Use GRID_2X3 (6 words) for older age groups (e.g. Grade 1 and up or age group 4-6). If age group is not provided, default to GRID_2X3."}, "components": [{"id": "word-{x}", "type": "text", "editable": true, "constraints": {"maxCharacters": 12}, "description": "One age-appropriate sight word to display for recognition/reading practice. Defaults to basic/beginner sight words if no grade is provided.", "semanticRole": "sightwords.item"}], "constraints": {"note": "Item count MUST exactly equal the selected layout's cell count — 4 items for GRID_1X4, 6 items for GRID_2X3. Never render a partially-filled grid.", "maxItems": 6, "minItems": 4, "itemCountMustEqual": "layout.cells"}}]}


### Backend Response strucutre for generate flashcards api

export interface SelectedTemplatePayload {
  id: string;
  name: string;
  description: string | null;
  templateType: string;
  layoutType: string;
  templateVersion: string;
  supportedAgeGroups: string[];
  supportedGrades: string[];
  learningObjectives: string[];
  subjectsSupported: string[];
  difficultyLevels: string[];
  tags: string[];
  pageSize: string;
  orientation: string;
  thumbnail: string | null;
  layoutDefinition: TemplateLayoutDefinition | unknown;
  active?: boolean;
}

export interface GenerateFlashcardsResponse {
  request: {
    query: string;
    topic: string;
    ageGroup: string;
    ageMin: number;
    ageMax: number;
    grade: string | null;
    subject: string | null;
    difficulty: string;
    language: string;
    learningObjective: string;
    educationalIntent: string;
    count: number;
  };
  selection: {
    ruleId: string;
    ruleName: string;
    score: number;
    priority: number;
  };
  template: SelectedTemplatePayload;
  templateVersion: string;
  layoutDefinition: TemplateLayoutDefinition | unknown;
  cards: FlashcardCardPayload[];
  metadata: {
    generatedAt: string;
    promptVersion: string;
    contentModel: string;
    imageConcurrency: number;
    requestId?: string;
    executionId?: string;
    correlationId?: string;
  };
  /** @deprecated Prefer metadata — kept for flashcards.html compatibility */
  renderingMetadata: {
    generatedAt: string;
    promptVersion: string;
    contentModel: string;
    imageConcurrency: number;
    requestId?: string;
    executionId?: string;
    correlationId?: string;
  };
  /** Server-rendered WebP card images and PDF output when rendering is enabled. */
  renderedOutput?: FlashcardRenderedOutput;
}

So match the id and show the UI as per layout. 